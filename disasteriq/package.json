{
  "name": "disasteriq-monorepo",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client --legacy-peer-deps --production=false && npm run build --prefix client && npm install --prefix server --legacy-peer-deps --production=false && npm run prisma:generate --prefix server",
    "install:all": "npm install --prefix client --legacy-peer-deps --production=false && npm install --prefix server --legacy-peer-deps --production=false"
  }
}
