{
  "name": "disasteriq-monorepo",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client --legacy-peer-deps && npm run build --prefix client && npm install --prefix server --legacy-peer-deps && npm run prisma:generate --prefix server",
    "install:all": "npm install --prefix client --legacy-peer-deps && npm install --prefix server --legacy-peer-deps"
  }
}
